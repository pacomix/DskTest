// Filename defines and its compressed/uncompressed size. -1 is added at the end for referencing the last byte when used as an offset.
// Also here is defined the maximum buffer for the zipped files.
#define ZIP_BUFFER_SIZE 1280
